import { Link, useNavigate } from "react-router-dom";
export default function Navbar(){const navigate=useNavigate();const user=JSON.parse(localStorage.getItem("lfUser")||"null");function logout(){localStorage.removeItem("lfUser");navigate("/");}return <header className="topbar"><Link className="brand" to="/">KFUPM Lost &amp; Found</Link><nav aria-label="Main navigation">{user?<><Link className="nav-link" to={`/${user.role}/dashboard`}>Dashboard</Link><Link className="nav-link" to="/owner/browse-items">Browse Found Items</Link><Link className="nav-link" to={`/${user.role}/notifications`}>Notifications</Link><Link className="nav-link" to={`/${user.role}/profile`}>Profile</Link><button className="nav-button" type="button" onClick={logout}>Logout</button></>:<><Link className="nav-link" to="/owner/browse-items">Browse Listings</Link><Link className="nav-link" to="/login">Login</Link></>}</nav></header>}
