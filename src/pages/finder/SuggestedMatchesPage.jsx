import AppLayout from "../../components/layout/AppLayout";import { foundItems,lostItems } from "../../data/mockData";
export default function SuggestedMatchesPage(){return <AppLayout role="finder"><section className="page"><div className="page-header"><div><h1>Suggested Matches</h1><p>Prototype matching based on category, location, and keywords.</p></div></div><div className="grid">{foundItems.map(found=>{const matches=lostItems.filter(l=>l.category===found.category||l.location===found.location);return <article className="card" key={found.id}><h2>{found.title}</h2><p className="muted">Found at {found.location} - {found.category}</p>{matches.length?<div className="responsive-grid" style={{marginTop:12}}>{matches.map(m=><div className="card" key={m.id}><h3>{m.title}</h3><p>{m.description}</p><p><span className="badge info">{m.category}</span> <span className="badge warning">{m.status}</span></p><button className="btn btn-primary btn-sm">Message Owner</button></div>)}</div>:<p className="muted">No strong matches yet.</p>}</article>})}</div></section></AppLayout>}
