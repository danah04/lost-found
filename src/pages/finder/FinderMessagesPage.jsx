import { useState } from "react";import AppLayout from "../../components/layout/AppLayout";import { messages as seed } from "../../data/mockData";
export default function FinderMessagesPage(){const[msgs,setMsgs]=useState(seed);const[text,setText]=useState("");function send(e){e.preventDefault();if(!text.trim())return;setMsgs([...msgs,{id:crypto.randomUUID(),sender:"You",text,time:"Now"}]);setText("")}return <AppLayout role="finder"><section className="page"><div className="page-header"><div><h1>Messages</h1><p>Secure conversation history between finder and item owner.</p></div></div><div className="card">{msgs.map(m=><div key={m.id} className={m.sender==="You"?"message-row mine":"message-row"}><div className="message-bubble"><strong>{m.sender}</strong><p>{m.text}</p><small>{m.time}</small></div></div>)}<form className="actions" onSubmit={send}><input style={{flex:1,minWidth:220}} value={text} onChange={e=>setText(e.target.value)} placeholder="Type your message"/><button className="btn btn-primary">Send</button></form></div></section></AppLayout>}
