import { Link } from "react-router-dom";import AppLayout from "../../components/layout/AppLayout";import { foundItems,lostItems } from "../../data/mockData";
export default function PendingListingsPage(){const pending=[...foundItems.filter(i=>i.status.includes("Pending")),...lostItems.slice(0,3).map(i=>({...i,status:"Pending"}))];return <AppLayout role="moderator"><section className="page"><div className="page-header"><div><h1>Pending Listings</h1><p>Approve, reject, or request clarification before publishing.</p></div></div><div className="table-wrap"><table><thead><tr><th>Title</th><th>Category</th><th>Date</th><th>Status</th><th>Actions</th></tr></thead><tbody>{pending.map(i=><tr key={i.id}><td>{i.title}</td><td>{i.category}</td><td>{i.date}</td><td><span className="badge warning">{i.status}</span></td><td><Link className="btn btn-sm btn-primary" to={`/moderator/review/${i.id}`}>Review</Link></td></tr>)}</tbody></table></div></section></AppLayout>}
