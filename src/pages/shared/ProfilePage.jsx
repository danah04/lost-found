import AppLayout from "../../components/layout/AppLayout";
export default function ProfilePage({role="owner"}){const user=JSON.parse(localStorage.getItem("lfUser")||"{}");return <AppLayout role={role}><section className="page"><div className="page-header"><div><h1>Profile</h1><p>Prototype account details from mock KFUPM SSO.</p></div></div><div className="card"><p><strong>Name:</strong> {user.name||"Demo User"}</p><p><strong>Role:</strong> {role}</p><p><strong>Email:</strong> {role}@kfupm.edu.sa</p><p className="muted">Real profile data will come from authentication and the backend later.</p></div></section></AppLayout>}
