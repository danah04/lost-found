import { useState } from "react";import AppLayout from "../../components/layout/AppLayout";import { reports as seed } from "../../data/mockData";
export default function ReportsPage(){const[reports,setReports]=useState(seed);function resolve(id,status){setReports(reports.map(r=>r.id===id?{...r,status}:r))}return <AppLayout role="moderator"><section className="page"><div className="page-header"><div><h1>Reports Center</h1><p>Review fraud, spam, misuse, duplicate, and inappropriate-content reports.</p></div></div><div className="table-wrap"><table><thead><tr><th>Target</th><th>Reported By</th><th>Reason</th><th>Status</th><th>Actions</th></tr></thead><tbody>{reports.map(r=><tr key={r.id}><td>{r.target}<br/><span className="muted">{r.timestamp}</span></td><td>{r.reportedBy}</td><td>{r.reason}</td><td><span className={r.status==="Open"?"badge warning":"badge success"}>{r.status}</span></td><td className="actions"><button className="btn btn-sm btn-secondary" onClick={()=>resolve(r.id,"Dismissed")}>Dismiss</button><button className="btn btn-sm btn-primary" onClick={()=>resolve(r.id,"Warning Issued")}>Issue Warning</button><button className="btn btn-sm btn-danger" onClick={()=>resolve(r.id,"Listing Removed")}>Remove Listing</button></td></tr>)}</tbody></table></div></section></AppLayout>}
