import { useState } from "react";import AppLayout from "../../components/layout/AppLayout";
const seed=[{id:"ret-1",item:"KFUPM ID Card",claimant:"Ahmed Al-Qahtani",finder:"Farah Al-Dosari",date:"2026-02-06",status:"Pending"},{id:"ret-2",item:"Blue Backpack",claimant:"Noura Al-Salem",finder:"Sara Al-Otaibi",date:"2026-02-05",status:"Pending"}];
export default function ReturnConfirmationPage(){const[rows,setRows]=useState(seed);const[method,setMethod]=useState("meetup");const[time,setTime]=useState("");function confirm(id){setRows(rows.map(r=>r.id===id?{...r,status:"Returned"}:r))}return <AppLayout role="moderator"><section className="page"><div className="page-header"><div><h1>Return Confirmation</h1><p>Confirm the item handover and close the case.</p></div></div><div className="card form" style={{marginBottom:16}}><div className="form-grid"><div className="field"><label>Return Method</label><select value={method} onChange={e=>setMethod(e.target.value)}><option value="meetup">Meetup</option><option value="admin">Handed to university office</option></select></div><div className="field"><label>Date and Time</label><input type="datetime-local" value={time} onChange={e=>setTime(e.target.value)}/></div></div></div><div className="table-wrap"><table><thead><tr><th>Item</th><th>Claimant</th><th>Finder</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.item}<br/><span className="muted">Claim date {r.date}</span></td><td>{r.claimant}</td><td>{r.finder}</td><td><span className={r.status==="Returned"?"badge success":"badge warning"}>{r.status}</span></td><td><button className="btn btn-sm btn-primary" onClick={()=>confirm(r.id)}>Confirm Return</button></td></tr>)}</tbody></table></div></section></AppLayout>}
