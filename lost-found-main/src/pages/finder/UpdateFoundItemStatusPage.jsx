import { useState } from "react";import { useParams,Link } from "react-router-dom";import AppLayout from "../../components/layout/AppLayout";import { foundItems } from "../../data/mockData";
export default function UpdateFoundItemStatusPage(){const{id}=useParams();const item=foundItems.find(i=>i.id===id)||foundItems[0];const[status,setStatus]=useState(item.status);const[notes,setNotes]=useState("");const[ok,setOk]=useState(false);return <AppLayout role="finder"><section className="page"><div className="page-header"><div><h1>Update Item Status</h1><p>Mark whether the item was returned or handed to administration.</p></div></div>{ok&&<div className="success-banner" style={{marginBottom:16}}>Item status updated to {status}.</div>}<form className="card form" onSubmit={e=>{e.preventDefault();setOk(true)}}><h2>{item.title}</h2><p>Current status: <span className="badge warning">{item.status}</span></p><div className="field"><label>Update Status <span className="required">*</span></label><select value={status} onChange={e=>setStatus(e.target.value)}><option>Returned</option><option>Handed to Administration</option><option>Recovered</option><option>Needs Moderator Review</option></select></div><div className="field"><label>Notes Optional</label><textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Add handover notes or location details."/></div><div className="actions"><button className="btn btn-primary">Confirm Update</button><Link className="btn btn-secondary" to="/finder/my-found-items">Cancel</Link></div></form></section></AppLayout>}
