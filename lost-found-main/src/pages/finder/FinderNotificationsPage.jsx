import { useState } from "react";import AppLayout from "../../components/layout/AppLayout";import { notifications as seed } from "../../data/mockData";
export default function FinderNotificationsPage(){const[items,setItems]=useState(seed);return <AppLayout role="finder"><section className="page"><div className="page-header"><div><h1>Notifications</h1><p>Approval updates, match alerts, status reminders, and messages.</p></div><button className="btn btn-secondary" onClick={()=>setItems(items.map(n=>({...n,read:true})))}>Mark All Read</button></div><div className="grid">{items.map(n=><article className="card" key={n.id}><h2>{n.title} {!n.read&&<span className="badge info">New</span>}</h2><p className="muted">{n.message}</p></article>)}</div></section></AppLayout>}
