import { useState } from "react";import { Link } from "react-router-dom";import AppLayout from "../../components/layout/AppLayout";import { foundItems } from "../../data/mockData";
export default function MyFoundItemsPage(){const[items,setItems]=useState(foundItems);function status(id,value){setItems(items.map(i=>i.id===id?{...i,status:value}:i))}return <AppLayout role="finder"><section className="page"><div className="page-header"><div><h1>My Found Items</h1><p>View details and update the status of items you reported.</p></div></div><div className="table-wrap"><table><thead><tr><th>Title</th><th>Category</th><th>Date Found</th><th>Status</th><th>Actions</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.title}</td><td>{i.category}</td><td>{i.date}</td><td><span className="badge warning">{i.status}</span></td><td className="actions"><Link className="btn btn-sm btn-outline" to={`/finder/found-items/${i.id}`}>View Details</Link><select value={i.status} onChange={e=>status(i.id,e.target.value)}><option>Pending Review</option><option>Approved</option><option>Returned</option><option>Handed to Administration</option><option>Recovered</option></select></td></tr>)}</tbody></table></div></section></AppLayout>}
